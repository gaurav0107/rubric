{
  "name": "rubric-monorepo",
  "version": "2.0.2",
  "private": true,
  "description": "Monorepo root for rubric — pairwise prompt evaluation framework",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "clean": "rm -rf packages/*/dist packages/*/.output packages/*/.svelte-kit"
  },
  "engines": {
    "bun": ">=1.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0",
    "vitest": "^4.1.5"
  }
}
